# 任意の作業ディレクトリに移動
cd ~/work

# リポジトリをクローン
git clone https://github.com/teshi0214/good_agent.git
cd good_agent

# 仮想環境を作成 & 有効化
python3 -m venv .venv
source .venv/bin/activate

# 必要ライブラリをインストール
pip install -r requirements.txt
